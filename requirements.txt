# Core notebook dependencies (PyTorch is installed by setup.ps1 with CPU/GPU selection)
"segment-geospatial[samgeo3]"  # provides the `samgeo` module incl. SamGeo3

numpy>=1.24
tqdm>=4.66
opencv-python>=4.8
rasterio>=1.3
matplotlib>=3.8

# Vector export
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
fiona>=1.9
